apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-stat
spec:
  serviceName: mysql
  replicas: 3
  selector:
    matchLabels:
      app: sql
  template:
    metadata:
      labels:
        app: sql
    spec:
      containers:
      - name: sql
        image: mysql
        ports:
        - containerPort: 3306
          name: web
        env:
         - name: MYSQL_ROOT_PASSWORD
           value: rootpassword
        volumeMounts:
        - name: www
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: www
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi